# poalim-IMDB-assignment-android

### Favorites ###
Favorites are stored locally on the device using SharedPreferences.
TMDB’s official favorites API requires a full user authentication/session flow, which I chose not to implement to keep the UX simple and avoid external login for this assignment.